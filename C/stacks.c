#include <stdio.h>
int main(){
/* stack - LIFO, last in first out, data structure
         - stores objects verticaly
         - push() to add, from bottom to top
         - pop() to grab, but first remove from the top, to grab the required one.
*/

stack()


return 0;}

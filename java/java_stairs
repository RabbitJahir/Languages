------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
public class p1_printing {
    public static void main(String[] args) {
       
       System.out.println("From p1_printing");
        System.out.println(1000);
        System.out.println(10 + 900);
        System.out.println(5000 / 2);

        // SINGLE LINE

        /*
         * multiLINE
         */

    }
}
------------------------
class akta container er moto, je ai name ta amar code contain kore, file name and class name must be same
public, private and default hoche access modifier.

public static void main(String[] args)   --- java er entry point
---------------------------------------
public, jvm accessable
static, java dont need to create object, without static java wont know how to run the program
void, return method is nothing
main, special method name the JVM searches for.
---------------------
java case sensitive
------------------
ln is new line, sentence shesh e new line
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-> all data types
---------------------------------------------------------------------
public class DataTypes{
    public static void main(String[] args) {
        
  // data_types variables = bits;

  // primitive
      byte b = 8; // 1 byte = 8 bit
      short s = 16;
      int i = 32;
      long l = 64;
      float f = 32.0f;
     // float f1 = 32.0; // error
      float f2 = 32;
      double d = 20.99;
      char c = 'A';
      boolean bool;
  // reference
      String str = "hello hookers";

      System.out.println(f); //just write the variables
      System.out.println("For printing and adding_" + str + "_add plus to the variable. Remeber to have spaces after and before a variable inside quotes."); 
	
    }
}
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
public class MathClass{
    public static void main(String[] args) {
        
      int a = 3, b = 8, c = -5;

      int min = Math.min(a,b);
      int max = Math.max(a,b);

      int min3 = Math.min(c, Math.min(a,b) );
      int max3 = Math.max(c, Math.max(a,b) );

      System.out.println(max3);

    //Math is a built in class

    System.out.println(Math.PI);
    System.out.println(Math.E);

    System.out.println(Math.sqrt(a));
    System.out.println(Math.pow(a, 3));
    System.out.println(Math.abs(30));

    System.out.println(Math.round(a));
    System.out.println(Math.floor(b));
    System.out.println(Math.ceil(c));

    }
}
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-> import java.util.Scanner;
--------------------------------------------------------------
import java.util.Scanner;

public class trying {
    public static void main(String[] args) {
        
      Scanner input = new Scanner(System.in);
 //data_type  variable =              keyboard theke input nibe

    int i = input.nextInt();
    double d = input.nextDouble();
    float f = input.nextFloat();
    float f1 = input.nextInt(); //input in int, but output in float

    input.nextLine(); // reads the newline character
    String s =input.nextLine();
    String word = input.next();

    System.out.println(word);

    input.close();
    }
}
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
->if-else
-> array code
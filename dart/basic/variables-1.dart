// only int and double
// strings, booleans,

//all data types are objects, so variables have default null, so no garbage values

void main() {
  int number = 10;
  int hexValue = 0xA;
  double xy = 10;
  double exponent = 6.69e2;
  String string = "string";

  print(
    "from int number: $number\n from hexValue: $hexValue\nfrom xy: $xy\nfrom exponent: $exponent\n from string: $string"
  );
}

/*
console.log(`Execution Context - The components and phases a code goes through before execution.
creation Phase - reads all codes and variables, sets up the memory needed. Does not read scopes.
executon Phase - ready to execute the variables with their data types and other codes, goes through scopes and restarts creation phase and executon phase inside scopes.`)
*/

/*
console.log(`Question 2`);
N = 20;
  for( let i=1;i<=N;i++)
  {
    if( i%2 !==0 && i%3!== 0)
      console.log(i);
  }
*/

/*
console.log(`Question 3);
let array = [2, 7, 5, 10, 3, 8]

const choose = 5;

let result = array.filter(num => num >= choose);
console.log(result);
*/

/*
console.log(`Question 4`);

function createPerson(){
  let name = `myself`;
  let age = 69;
  let city = `Galaxy-M64`;

  return {name, age, city};
}

let person = createPerson();

console.log(person.name);
*/